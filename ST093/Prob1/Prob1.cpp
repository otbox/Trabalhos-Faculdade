#include <bits/stdc++.h>
using namespace std;


const int MAXN = 1e5 + 5;
vector<int> adj[MAXN];
vector<int> visited(MAXN);


int calcular(string D) {
    
    if ()
}

int main (){
    ios::sync_with_stdio();
    cin.tie(0);

    int N = 0;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        string dis;
        cin >> dis;
    }
    

    string x;
    while (cin >> x) {
        if (x == "sair"){
            return 0;
        }
        
    }
 }