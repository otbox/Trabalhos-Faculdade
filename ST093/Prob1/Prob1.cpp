#include <bits/stdc++.h>
using namespace std;


int main (){
    ios::sync_with_stdio();
    cin.tie(0);

    int N = 0;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        scanf("%c:%c",)
    }
    

    string x;
    while (cin >> x) {
        if (x == "sair"){
            return 0;
        }
        
    }
 }